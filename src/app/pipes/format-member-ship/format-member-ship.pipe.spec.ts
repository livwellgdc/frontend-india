import { FormatMemberShipPipe } from './format-member-ship.pipe';

describe('FormatMemberShipPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMemberShipPipe();
    expect(pipe).toBeTruthy();
  });
});
